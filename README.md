# PasswordStrength
A C-based console application that generates strong passwords and checks password strength. It ensures passwords include uppercase, lowercase, digits, and symbols, and categorizes strength as Weak, Medium, or Strong based on length and complexity.
